{
  "name": "zenkai-stream-proxy",
  "version": "2.2.1",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}

